%h1= @category.name
%table
  %thead
    %tr
      %th.th-name Name
      %th.th-srp SRP
      -#%th Description
      %th{:colspan => "2"}
  %tbody
    - @category.items.each do |item|
      %tr
        %td= link_to item.name, item
        %td= item.price

